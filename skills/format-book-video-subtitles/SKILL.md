---
name: format-book-video-subtitles
description: 将图书短视频长文案切分为适合剪辑软件的无标点纯文本字幕，每行优先7至9字且绝不超过9字，并保证完整书名独占一行。当用户要求生成字幕、字幕分行、去标点、九字字幕或剪辑软件可用字幕文本时使用。
---

# 图书短视频字幕排版

## 输入

接收书名和完整口播文案。书名用于识别文案中的完整标题，不能凭空添加到没有书名的文案中。

## 切分规则

- 删除全部标点符号，但不删除原文中的汉字、数字、英文字母及有意义内容。
- 每行以7至9个字符为最佳，普通行绝不超过9个字符。
- 在自然语义停顿或配音气口处切分。
- 不在完整词语、人名、地名、数字、年代、固定概念中间切分。
- 助词“的”不得位于行首。
- 每个字幕短句单独一行，不插入序号、项目符号、解释或空白装饰。
- 保持原文内容顺序和叙事逻辑，不改写、不概括。

## 书名特例

- 完整书名必须保持在同一行，且该行只放书名。
- 书名超过9个字符时允许该行超过9字。
- 书名在正文中每次完整出现时均按同一规则处理。
- 不把书名号保留到输出中。

## 终检

静默逐行检查：

- 除书名行外是否全部不超过9个字符。
- 是否优先达到7至9字。
- 是否有标点残留。
- 是否出现以“的”开头的行。
- 是否切断完整词语或遗漏、重复原文内容。

## 输出

仅使用一个代码块输出纯文本字幕，一句一行。不要在代码块内外添加统计、解释或其他文字。
